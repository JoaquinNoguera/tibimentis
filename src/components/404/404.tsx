import * as React from 'react';
import './styles.scss';

const NotFound = () => {
    return(
        <div id="not-found">
            <h1> Error! </h1>
            <h1>
                Lo sentimos, la página que desea buscar no existe
            </h1>
        </div>
    );
}

export default NotFound;
